import { NgxStickyPlusDirective } from './ngx-sticky-plus.directive';

describe('NgStickyEventDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxStickyPlusDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
